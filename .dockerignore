# Installed fresh inside the image
node_modules
**/node_modules

# Build artifacts
apps/web/.next
apps/web/out
**/dist
**/build
**/*.tsbuildinfo
.turbo
**/.turbo

# Secrets — provided as Render environment variables, never baked into the image
.env
.env.*
**/.env
**/.env.*

# VCS / editor / OS
.git
.gitignore
.idea
.vscode
**/.DS_Store

# Docs & local-only files
*.md
!packages/**/*.md
